# NOTE: This Dockerfile was originally created for AWS Fargate deployment.
# The project has since migrated to EC2 for processing needs.
# Keeping this file for potential future use cases with containerized workflows.


FROM public.ecr.aws/lambda/python:3.12

# Install system dependencies
RUN yum update -y && yum install -y gcc gcc-c++ make

# Copy requirements and install Python packages
COPY requirements.txt .
RUN pip install -r requirements.txt

# Copy Lambda function code
COPY lambda_function.py ${LAMBDA_TASK_ROOT}

CMD ["lambda_function.lambda_handler"]